name: CI | Test
on:
  workflow_call:

jobs:
  build-everything:
    strategy:
      matrix:
        vmm:
          - qemu
          - clh
    env:
      VMM: ${{ matrix.vmm }}
    steps:
      - name: Build step
        run: |
          true

