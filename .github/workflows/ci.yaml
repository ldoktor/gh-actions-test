name: Run the CI
on:
  workflow_call:
    inputs:
      commit-hash:
        required: true
        type: string
      pr-number:
        required: true
        type: string
      tag:
        required: true
        type: string
      target-branch:
        required: false
        type: string
        default: ""

jobs:
  build:
    runs-on: ubuntu-latest
    uses: ./.github/workflows/build.yaml

  test:
    needs: build
    runs-on: ubuntu-latest
    uses: ./.github/workflows/test.yaml
